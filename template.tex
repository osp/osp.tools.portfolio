%\showframe 
%\showgrid

% LANGUAGE {{{
    \setuplanguage[fr][
        leftquote=\upperleftdoublesixquote,
        rightquote=\upperrightdoublesixquote,
    ]
    \mainlanguage[fr]
    \setcharacterspacing [frenchpunctuation]
% }}}

% LAYOUT {{{
    \setuplanguage[fr]
    \setupcolumns[n=2, balance=yes]
    \setuplayout[
            topspace=25mm,
            backspace=40mm,
            height=232mm,
            width=154mm,
            header=0pt, 
            columns=2,
            grid=yes, % activates baseline grid
            ] % page layout
    \setuptolerance[{verystrict, verystrict}]

    \setuppagenumbering[location={}]  % No page numbering
% }}}

% TYPOGRAPHY {{{
    \usemodule[simplefonts]
    \setmainfont[osphelveticaserifsounds]
    \setupbodyfont[osphelveticaserifsounds, 9pt]
    \definebodyfontenvironment[default][x=0.75 ,a=1, b=1.75, c=2, d=3] % defines higher sizes of fonts 
    \setupwhitespace[12pt]  % inter-paragraph spacing
    \setupinterlinespace[12pt]
% }}}

% JUSTIFICATION {{{
    \setuptolerance[verytolerant] % justification settings
%    \interlinepenalty=8000 % tries to avoid widows
%    \widowpenalty=8000 % tries to avoid widows
%    \clubpenalty=8000 % tries to avoid widows
%    \doublehyphendemerits=1000000 % tries to avoid consecutive hyphenations
%    \setupfonthandling [hz] [min=50,max=50,step=10]
%    \setupalign[hz, hanging]
% }}}

% TITLES {{{
    \setupheader[text][align=flushleft]

    \setuphead[subject][
        sectionnumber=no,
	align={flushleft, outer, nothyphenated, verytolerant, stretch},
        %align=center,
        before=\page,
        style=\tfd,
        after={\blank[3*big]},
        ]


% OTHER {{{
\setupitems[after={\blank[none]}]
\setupitemgroup[itemize][after={\blank[none]},inbetween={\blank[none]}]
\setupdelimitedtext[quotation][1][
    before={\startnarrower[left]},
    after={\stopnarrower},
    ]
\setupdelimitedtext[legend][1][
    before={
        \startnarrower[left, right]
    },
    after={
        \stopnarrower
    }
]
\setupexternalfigures[maxwidth=\textwidth - 70pt]
\setupnarrower[middle=35pt, left=35pt, right=0pt, before={\blank[none]}]

\definebar[underline][
    order=background, % foreground to put line in front
    rulethickness=0.12, 
    offset=-0.8, 
    continue=yes, 
    color=black,
    ]
\definedescription[descr][
    width=30pt,
    hang=5,
    indentnext=yes,
    before={\startalignment[middle, nothyphenated, verytolerant]},
    after={\stopalignment\blank[none]},
    headstyle={\underline},
    style={\underline},
    ]
\setuphead[subsubject][
    command=\MyChapterCommand,
    ]
\setupheadtext[chapter=Chapter] % used by \headtext
\setupfootnotes[
    location=text,
    ]



% TITLES
% \definedescription[title][
%      headstyle={\setupbodyfont[umepgothicvertical, 6pt]},
%      style={\setupbodyfont[umemincho, 8pt]},
%      align=flushleft,
%      location=top,
%      %hang=20,
%      command=\hskip-3mm, 
%      margin=3mm,
%      %width=fit,
%      before={\setupinterlinespace[10pt]},
%      ]



% }}}

\starttext
\startalignment[flushleft]
%\useexternalfigure[dummy][nofile][width=\textwidth - 50pt]
\startcolumns[n=2]
\startalignment[flushleft]

\input input.tex
\stopalignment
\stopcolumns
\stopalignment
\stoptext

% vim: set foldmethod=marker :





